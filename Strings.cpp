#include<bits/stdc++.h>
using namespace std;

int main(){
    string s="AdvaitAxiom";
    cout<<s[6]<<s[7];
    int len=s.length();
    int len1 = s.size();
    cout<<endl<<len<<endl<<len1;
    cout<<endl<<s[len-1];
    //cout<<s[32];
 return 0;
}
